node_modules
dist
.venv-img
.vercel

# Environment & secrets
.env
.env.local
.env.*.local
.env.production
.env*.production
server/.env
telegram-notify-bot/.env

# DB dumps (never commit PII)
server/data/migration-export.json
**/migration-export.json

# OS & IDE
.DS_Store
**/.DS_Store

# Local database & uploads (Render uses its own storage)
server/data/*.sqlite
server/data/*.sqlite-shm
server/data/*.sqlite-wal
server/uploads/

# Google Service Account keys (never commit)
server/data/google-sheets-key.json
**/google-sheets-key.json
